# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: c38bd5ea-5617-44dd-bfe2-53915c7072fe

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
    default: "citibike_dev"
  environment:                          # ✅ ADD THIS
    description: Environment name for storage account
    default: "d"
  dev_flag:
    description: Flag to indicate dev environment
    default: false


targets:
  DEFAULT:
    mode: development
    default: true
    workspace:
      host: https://adb-7405618929302580.0.azuredatabricks.net
      # DEFAULT now deploys to user's personal folder
      root_path: ~/. databricks/bundle/${bundle.name}/${bundle.target}
      
    variables:
      catalog: citibike_dev
      environment:  "d"
      
    presets:
      artifacts_dynamic_version: true

  dev:
    mode: production
    workspace:
      host: https://adb-7405618929302580.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/zedbelachew@zbelachewarchitectoutlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/skylink/
    variables:
      catalog:
        default: "citibike_dev"
      environment: "u"  # Add this - for skylinkteastus2datalake
      dev_flag:
        default: false
    permissions:
      - service_principal_name: 926fc39c-76ae-490f-bbce-482878702ea9
        level: CAN_MANAGE
    run_as:
      service_principal_name: 926fc39c-76ae-490f-bbce-482878702ea9

  test:
    mode: production
    workspace:
      host: https://adb-7405609017722464.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/zedbelachew@zbelachewarchitectoutlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/skylink/
    variables:
      catalog:
        default: "citibike_test"
      environment: "u"  # Add this - for skylinkteastus2datalake
      dev_flag:
        default: false
    permissions:
      - service_principal_name: c110fe01-7967-41ac-a4c8-b9225bed7856
        level: CAN_MANAGE
    run_as:
      service_principal_name: c110fe01-7967-41ac-a4c8-b9225bed7856

  prod:
    mode: production
    workspace:
      host: https://adb-7405613130773209.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/zedbelachew@zbelachewarchitectoutlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/skylink/
    variables:
      catalog:
        default: "citibike_prod"
      environment:  "p"
      dev_flag:
        default: false
    permissions:
      - service_principal_name: b056d4f5-5245-477a-a64c-e4f4e74c85e4
        level: CAN_MANAGE
    run_as:
      service_principal_name: b056d4f5-5245-477a-a64c-e4f4e74c85e4


     
