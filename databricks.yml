# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: c38bd5ea-5617-44dd-bfe2-53915c7072fe

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
    default: "citibike_dev"
  environment:                          # ✅ ADD THIS
    description: Environment name for storage account
    default: "d"
  dev_flag:
    description: Flag to indicate dev environment
    default: false


targets:
  DEFAULT:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    default: true
    workspace:
      host: https://adb-7405615715285808.8.azuredatabricks.net
      root_path: /Workspace/Shared/skylink/
      
    variables:
      catalog: citibike_dev
      environment:
        description: Environment name for storage account
        default: "d" 
      
    presets:
      artifacts_dynamic_version: true

  test:
    mode: production
    workspace:
      host: https://adb-7405614336867548.8.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/zedbelachew@zbelachewarchitectoutlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog:
        default: "citibike_test"
      environment: "u"  # Add this - for skylinkteastus2datalake
      dev_flag:
        default: false
    permissions:
      - service_principal_name: f1a5fcbc-a0bb-46f9-9a4e-c76c18bf5a36
        level: CAN_MANAGE
    run_as:
      service_principal_name: f1a5fcbc-a0bb-46f9-9a4e-c76c18bf5a36

  prod:
    mode: production
    workspace:
      host: https://adb-7405610128763595.15.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/zedbelachew@zbelachewarchitectoutlook.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog:
        default: "citibike_prod"
      environment:  "p"
      dev_flag:
        default: false
    permissions:
      - service_principal_name: d6675f60-dfa0-4a53-9457-f159708dbe87
        level: CAN_MANAGE
    run_as:
      service_principal_name: d6675f60-dfa0-4a53-9457-f159708dbe87


     
